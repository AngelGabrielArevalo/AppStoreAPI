export enum Rol {
    USER = 'USER',
    CUSTOMER = 'CUSTOMER',
    ADMIN = 'ADMIN',
}

export enum SchemaDto {
    CREATE =  'CREATE',
    UPDATE = 'UPDATE', 
}

export enum LocationRequest {
    'PARAMS' = 'params',
    'QUERY' = 'query',
    'BODY' = 'body',
}